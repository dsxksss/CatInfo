diff --git a/dist/index.cjs b/dist/index.cjs
index 4709730f1d6608dee4ffb3a386d1732f776c5f5c..d2d50f55663bbd40c030847406722f76d68a7f75 100644
--- a/dist/index.cjs
+++ b/dist/index.cjs
@@ -909,8 +909,8 @@ function drawTimeAxis(ctx, layout, palette, windowSecs, targetWindowSecs, format
 }
 
 // src/draw/orderbook.ts
-var GREEN = [34, 197, 94];
-var RED = [239, 68, 68];
+var GREEN = [239, 68, 68];
+var RED = [34, 197, 94];
 function createOrderbookState() {
   return {
     labels: [],
@@ -1116,10 +1116,10 @@ function drawParticles(ctx, state, dt) {
 }
 
 // src/draw/candlestick.ts
-var BULL = "#22c55e";
-var BEAR = "#ef4444";
-var BULL_RGB = [34, 197, 94];
-var BEAR_RGB = [239, 68, 68];
+var BULL = "#ef4444";
+var BEAR = "#22c55e";
+var BULL_RGB = [239, 68, 68];
+var BEAR_RGB = [34, 197, 94];
 function blendColor2(t) {
   const r = Math.round(BEAR_RGB[0] + (BULL_RGB[0] - BEAR_RGB[0]) * t);
   const g = Math.round(BEAR_RGB[1] + (BULL_RGB[1] - BEAR_RGB[1]) * t);
@@ -2193,14 +2193,22 @@ function updateBadgeDOM(badge, cfg, smoothValue, layout, momentum, badgeY, badge
     if (!cfg.showMomentum) {
       fillColor = cfg.palette.line;
     } else {
-      const target = momentum === "up" ? 1 : momentum === "down" ? 0 : bs.green;
-      bs.green = noMotion ? target : lerp(bs.green, target, MOMENTUM_COLOR_LERP, dt);
-      if (bs.green > 0.99) bs.green = 1;
-      if (bs.green < 0.01) bs.green = 0;
-      const g = bs.green;
-      const rr = Math.round(MOMENTUM_RED[0] + (MOMENTUM_GREEN[0] - MOMENTUM_RED[0]) * g);
-      const gg = Math.round(MOMENTUM_RED[1] + (MOMENTUM_GREEN[1] - MOMENTUM_RED[1]) * g);
-      const bb = Math.round(MOMENTUM_RED[2] + (MOMENTUM_GREEN[2] - MOMENTUM_RED[2]) * g);
+      const pct = Math.max(0, Math.min(1, (smoothValue - layout.minVal) / (layout.valRange || 1)));
+      const GREEN_COLOR = [34, 197, 94];
+      const AMBER_COLOR = [245, 165, 36];
+      const RED_COLOR = [239, 68, 68];
+      let rr, gg, bb;
+      if (pct < 0.5) {
+        const t = pct / 0.5;
+        rr = Math.round(GREEN_COLOR[0] + (AMBER_COLOR[0] - GREEN_COLOR[0]) * t);
+        gg = Math.round(GREEN_COLOR[1] + (AMBER_COLOR[1] - GREEN_COLOR[1]) * t);
+        bb = Math.round(GREEN_COLOR[2] + (AMBER_COLOR[2] - GREEN_COLOR[2]) * t);
+      } else {
+        const t = (pct - 0.5) / 0.5;
+        rr = Math.round(AMBER_COLOR[0] + (RED_COLOR[0] - AMBER_COLOR[0]) * t);
+        gg = Math.round(AMBER_COLOR[1] + (RED_COLOR[1] - AMBER_COLOR[1]) * t);
+        bb = Math.round(AMBER_COLOR[2] + (RED_COLOR[2] - AMBER_COLOR[2]) * t);
+      }
       fillColor = `rgb(${rr},${gg},${bb})`;
     }
     badge.path.setAttribute("fill", fillColor);
diff --git a/dist/index.js b/dist/index.js
index fd0506272e9a54432a0882c3a389ee74302e4718..2ebc60216d69608dd8097e4e7865dc70fa2df4c0 100644
--- a/dist/index.js
+++ b/dist/index.js
@@ -882,8 +882,8 @@ function drawTimeAxis(ctx, layout, palette, windowSecs, targetWindowSecs, format
 }
 
 // src/draw/orderbook.ts
-var GREEN = [34, 197, 94];
-var RED = [239, 68, 68];
+var GREEN = [239, 68, 68];
+var RED = [34, 197, 94];
 function createOrderbookState() {
   return {
     labels: [],
@@ -1089,10 +1089,10 @@ function drawParticles(ctx, state, dt) {
 }
 
 // src/draw/candlestick.ts
-var BULL = "#22c55e";
-var BEAR = "#ef4444";
-var BULL_RGB = [34, 197, 94];
-var BEAR_RGB = [239, 68, 68];
+var BULL = "#ef4444";
+var BEAR = "#22c55e";
+var BULL_RGB = [239, 68, 68];
+var BEAR_RGB = [34, 197, 94];
 function blendColor2(t) {
   const r = Math.round(BEAR_RGB[0] + (BULL_RGB[0] - BEAR_RGB[0]) * t);
   const g = Math.round(BEAR_RGB[1] + (BULL_RGB[1] - BEAR_RGB[1]) * t);
@@ -2166,14 +2166,22 @@ function updateBadgeDOM(badge, cfg, smoothValue, layout, momentum, badgeY, badge
     if (!cfg.showMomentum) {
       fillColor = cfg.palette.line;
     } else {
-      const target = momentum === "up" ? 1 : momentum === "down" ? 0 : bs.green;
-      bs.green = noMotion ? target : lerp(bs.green, target, MOMENTUM_COLOR_LERP, dt);
-      if (bs.green > 0.99) bs.green = 1;
-      if (bs.green < 0.01) bs.green = 0;
-      const g = bs.green;
-      const rr = Math.round(MOMENTUM_RED[0] + (MOMENTUM_GREEN[0] - MOMENTUM_RED[0]) * g);
-      const gg = Math.round(MOMENTUM_RED[1] + (MOMENTUM_GREEN[1] - MOMENTUM_RED[1]) * g);
-      const bb = Math.round(MOMENTUM_RED[2] + (MOMENTUM_GREEN[2] - MOMENTUM_RED[2]) * g);
+      const pct = Math.max(0, Math.min(1, (smoothValue - layout.minVal) / (layout.valRange || 1)));
+      const GREEN_COLOR = [34, 197, 94];
+      const AMBER_COLOR = [245, 165, 36];
+      const RED_COLOR = [239, 68, 68];
+      let rr, gg, bb;
+      if (pct < 0.5) {
+        const t = pct / 0.5;
+        rr = Math.round(GREEN_COLOR[0] + (AMBER_COLOR[0] - GREEN_COLOR[0]) * t);
+        gg = Math.round(GREEN_COLOR[1] + (AMBER_COLOR[1] - GREEN_COLOR[1]) * t);
+        bb = Math.round(GREEN_COLOR[2] + (AMBER_COLOR[2] - GREEN_COLOR[2]) * t);
+      } else {
+        const t = (pct - 0.5) / 0.5;
+        rr = Math.round(AMBER_COLOR[0] + (RED_COLOR[0] - AMBER_COLOR[0]) * t);
+        gg = Math.round(AMBER_COLOR[1] + (RED_COLOR[1] - AMBER_COLOR[1]) * t);
+        bb = Math.round(AMBER_COLOR[2] + (RED_COLOR[2] - AMBER_COLOR[2]) * t);
+      }
       fillColor = `rgb(${rr},${gg},${bb})`;
     }
     badge.path.setAttribute("fill", fillColor);
